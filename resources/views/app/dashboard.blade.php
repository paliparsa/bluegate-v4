@extends('layouts.app')
@section('title','داشبورد — BlueGate')
@section('content')
<div class="top-title"><div><h2>داشبورد</h2><div class="muted">نمای کلی حساب و سرویس‌ها</div></div><a class="btn primary" href="{{ route('app.buy') }}">خرید سرویس</a></div><div class="stats"><div class="card stat"><span class="muted">موجودی کیف پول</span><b>{{ number_format((float)($wallet->balance_cached ?? 0)) }}</b><span class="muted">تومان</span></div><div class="card stat"><span class="muted">سرویس فعال</span><b>{{ $active }}</b><span class="muted">سرویس</span></div><div class="card stat"><span class="muted">سفارش‌ها</span><b>{{ $orders->count() }}</b><span class="muted">اخیر</span></div><div class="card stat"><span class="muted">وضعیت شبکه</span><b style="color:var(--ok);font-size:20px">Operational</b></div></div><div class="section"><div class="top-title"><h2>آخرین سرویس‌ها</h2><a class="muted" href="{{ route('app.services') }}">مشاهده همه</a></div><div class="card table-wrap">@if($services->count())<table class="table"><tr><th>شناسه</th><th>وضعیت</th><th>مصرف</th><th>انقضا</th></tr>@foreach($services as $s)<tr><td><a href="{{ route('app.services.show',$s->id) }}">{{ substr($s->id,0,8) }}</a></td><td>{{ $s->status }}</td><td>{{ number_format(($s->traffic_used_bytes ?? 0)/1073741824,1) }} GB</td><td>{{ $s->expires_at ?? '—' }}</td></tr>@endforeach</table>@else<div class="empty">هنوز سرویسی نداری. <a style="color:var(--blue)" href="{{ route('app.buy') }}">اولین سرویس را بخر</a></div>@endif</div></div>
@endsection

