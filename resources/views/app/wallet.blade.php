@extends('layouts.app')
@section('title','کیف پول — BlueGate')
@section('content')<div class="top-title"><div><h2>کیف پول</h2><div class="muted">دفتر مالی حساب</div></div></div><div class="card" style="max-width:420px"><span class="muted">موجودی فعلی</span><div class="price">{{ number_format((float)($wallet->balance_cached ?? 0)) }} تومان</div><button class="btn primary" disabled>افزایش موجودی — فاز پرداخت</button></div><div class="section"><h2 style="font-size:22px">تراکنش‌ها</h2><div class="card table-wrap">@if(method_exists($tx,'count') && $tx->count())<table class="table"><tr><th>نوع</th><th>مبلغ</th><th>جهت</th><th>تاریخ</th></tr>@foreach($tx as $t)<tr><td>{{ $t->type }}</td><td>{{ number_format((float)$t->amount) }}</td><td>{{ $t->direction }}</td><td>{{ $t->created_at }}</td></tr>@endforeach</table>@else<div class="empty">تراکنشی ثبت نشده است.</div>@endif</div></div>@endsection
