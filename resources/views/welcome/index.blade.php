@extends('layouts.base')
@section('title','BlueGate — اینترنت پایدار و هوشمند')
@section('body')<main><section class="container hero"><div><div class="eyebrow">BLUEPING NETWORK</div><h1>سرویس شبکه‌ای که<br>خودش را مدیریت می‌کند.</h1><p>خرید، تمدید، مدیریت حجم و Subscription در یک پنل ساده. زیرساخت BlueGate برای مدیریت چند نود و تحویل خودکار سرویس طراحی شده است.</p><div style="display:flex;gap:10px;margin-top:24px"><a class="btn primary" href="{{ auth()->check()?route('app.buy'):route('register') }}">شروع کنید</a><a class="btn" href="{{ route('status') }}">وضعیت شبکه</a></div></div><div class="hero-card"><span class="pill">BlueGate Network</span><div class="metric"><span>Subscription API</span><b style="color:var(--ok)">Operational</b></div><div class="metric"><span>Provisioning Core</span><b style="color:var(--ok)">Ready</b></div><div class="metric"><span>Multi-node Architecture</span><b>V4</b></div></div></section><section class="container section"><h2>پلن‌ها</h2><div class="section-lead">پلن‌های اولیه قابل مدیریت از دیتابیس و پنل ادمین هستند.</div><div class="grid">@forelse($products as $product)<article class="card"><span class="pill">{{ $product->category }}</span><h3 style="margin-top:14px">{{ $product->name }}</h3><p class="muted">{{ $product->description }}</p>@php($first=$plans->get($product->id)?->first())@if($first)<div class="price">از {{ number_format((float)$first->base_price) }} تومان</div>@endif<a class="btn primary" href="{{ auth()->check()?route('app.buy'):route('register') }}">انتخاب پلن</a></article>@empty<div class="card">هنوز پلنی تعریف نشده است.</div>@endforelse</div></section></main>
@endsection
