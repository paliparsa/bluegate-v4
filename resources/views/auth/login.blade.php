@extends('layouts.base')
@section('title','ورود — BlueGate')
@section('body')<div class="auth-wrap"><form class="card auth" method="post" action="{{ route('login.post') }}">@csrf<h2>ورود به BlueGate</h2><p class="muted">به پنل مدیریت سرویس‌های خودت وارد شو.</p>@if($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<div class="field"><label>ایمیل</label><input class="input" type="email" name="email" value="{{ old('email') }}" required></div><div class="field"><label>رمز عبور</label><input class="input" type="password" name="password" required></div><label class="muted"><input type="checkbox" name="remember"> مرا به خاطر بسپار</label><button class="btn primary" style="width:100%;margin-top:18px">ورود</button><p class="muted">حساب نداری؟ <a style="color:var(--blue)" href="{{ route('register') }}">ثبت‌نام</a></p></form></div>@endsection
