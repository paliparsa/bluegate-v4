@extends('layouts.base')
@section('title','ثبت‌نام — BlueGate')
@section('body')<div class="auth-wrap"><form class="card auth" method="post" action="{{ route('register.post') }}">@csrf<h2>ساخت حساب</h2><p class="muted">برای خرید و مدیریت سرویس‌ها حساب بساز.</p>@if($errors->any())<div class="alert">{{ $errors->first() }}</div>@endif<div class="field"><label>نام</label><input class="input" name="name" value="{{ old('name') }}" required></div><div class="field"><label>ایمیل</label><input class="input" type="email" name="email" value="{{ old('email') }}" required></div><div class="field"><label>شماره موبایل (اختیاری)</label><input class="input" name="phone" value="{{ old('phone') }}"></div><div class="field"><label>رمز عبور</label><input class="input" type="password" name="password" required></div><div class="field"><label>تکرار رمز</label><input class="input" type="password" name="password_confirmation" required></div><button class="btn primary" style="width:100%">ساخت حساب</button></form></div>@endsection
